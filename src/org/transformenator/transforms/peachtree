;
; Transform for Peachtree word processor
;
;   First, remove controls that conflict with RTF specification (this peachtree transform) - leaving plain text with some decoration
;     Note - call tranformutil with a null suffix (i.e. transformutil peachtree Originals Step1 "")
;   Next, use the peachtree_rtf transform to pretty the files up
;     (i.e. transformutil peachtree_rtf Step1 Converted)
;

; Remove controls - lines beginning with a backslash
regex = @\\.*\r\n@@

; EOF is ctrl-Z
1a = "{@@<FiLe_EoF>@@}"